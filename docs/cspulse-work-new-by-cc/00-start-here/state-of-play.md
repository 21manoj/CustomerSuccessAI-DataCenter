# CS Pulse — state of play

Verified against live endpoints, customer 390/393/401, 2026-08-22 (evening pass — item 20's xfail marker, Track B's items e/f, both deployed). All items below are merged to `main` (`1e711f6da`, pushed) — no longer feature-branch-only work. Supersedes the running commentary; this is the only document you need to re-enter the work.

**2026-08-27 update (this file + its companion docs brought under git control, at `docs/cspulse-work-new-by-cc/` in-repo — no more hunting `~/Downloads` for them):** Track C (WS-2 edge provenance) is now fully shipped through 2g — see the Track C section below, rewritten. Item 25 (Po1 re-tier) shipped. New items 28-31 added. See "## 2026-08-27 additions" near the end for full detail, verified live on two fresh tenants (customer 411 dc2_s, customer 412 datacenter_v1) plus direct DB/tracer checks on the live EC2 box. **Correction to how this doc's own history was almost mis-cited today:** `ws2/item26_two_models_decision.md` and `ws2/bucket_divergence_decision.md` read, on their own, like an open "owner decision, not applied" — they are not; item 26/27 below already show this shipped 2026-08-24 (`581be26ba`). Those two files are the working notes that led to that already-applied decision, kept for history. Always cross-check this file before treating a decision doc's own header as current status.

An independent verification pass (separate tool, separate session, referred to below as "CC-review") checked this document's claims against live endpoints before this revision. Its findings are folded in below rather than kept as a separate document — see the per-item notes for what it confirmed, what it couldn't reach, and what it corrected.

---

## Fixed and verified live

| # | finding | evidence it's fixed |
|---|---|---|
| 1 | `get_vertical_config` NameError | returns 6 pillars, 38 KPIs, correct datacenter_v1 names |
| 2 | `get_kpi_catalog(390)` served dc2_s config | now 6 pillars / 38 KPIs, P4 = Power & Facility, `default_pillar_weights_l2` matches `customer_pillar_weights_l2` exactly. Hardcoded if/else fully deleted — delegates to `utils.vertical_registry`, raises rather than falling through. CC-review confirmed live: `get_kpi_catalog(999999)` → *"Customer 999999 not found"*, not a silent dc2_s default. |
| 3 | `partner_portal` exposed facility data as partner data | now refuses: *"vertical has no pillar in the 'partner' role..."*. CC-review confirmed the gate holds on **both** `scorecard` and `impact` actions — an earlier-style fix might have gated only the default action. |
| 4 | CFO `pillar_investments` showed dc2_s names, dropped P6 | now all six datacenter_v1 pillars |
| 14 | password-login and magic-link routed the same customer to two different dashboard families | both routes now call one shared resolver (`app_v3_minimal.py`); magic-link's old crude `'saas' in vertical.lower()` check mis-routed healthcare_provider/manufacturing_iot to `datacenter`. Found while verifying #3, not in the original audit. |
| 15 | `id_generator` raised for any vertical outside a hardcoded prefix dict, blocking onboarding for a new vertical entirely | now derives a prefix from the registry's `SUPPORTED_VERTICALS` when unlisted. |
| 16 | CFO `roi_scaling` block returned `current_roi: 0` and all-zero projections for any customer with real playbook spend | `resolve_cfo_roi_pct()` was called with `estimated_investment` — deliberately zeroed whenever real spend exists — instead of `effective_investment`, computed two lines earlier and already used correctly elsewhere. Verified live on customer 393 (real $325K spend): `current_roi` 0 → 310, projections nonzero. CC-review independently re-verified on customer 390 (real $331,120 spend): `roi_pct` 0 → 302, `roi_multiple` 0 → 4, projections 0/0/0 → 469/699/996, growth bars 0/0/0 → 47/70/100. Math checks: `1,331,250 / 331,120 = 4.02` → net 302%. The scaling chart renders instead of three empty bars. |
| 17 | Po1 "Growth" double-count (was item 9, below) | three of the six Power-of-1 metrics share a playbook with another metric (PB-01 → TTFV + product_adoption, PB-02 → GRR + ticket_resolution_time, PB-04 → NRR + expansion_rate); summing all six for `layered_story`'s Growth layer counted each shared playbook's benefit twice. `power_of_1_model.dedupe_portfolio_dollar_impact()` now keeps the larger figure per shared playbook. Verified live on customer 393: Growth layer 1,746,250 → 1,331,250. CC-review independently re-verified on 390, same arithmetic (`max(306250,125000) + max(500000,190000) + max(525000,100000)` = 1,331,250), and traced the propagation: it correctly reached `roi_impact`, `total_value` (−415,000), `efficiency_score` (64→48), `rev_per_cs_dollar` (5.3→4), and per-account `impact`/`roi_pct` (427→302, see item 6 below — the per-account constant moved, the constancy itself is unchanged, still open). **Residual — now a live divergence, not a latent one (item 22 below):** `pillar_investments`' own impact sum still totals the undeduped 1,746,250. Before this fix every surface agreed on 1,746,250 (consistent and wrong); now two say 1,331,250 and one says 1,746,250 (inconsistent, one correct, nothing marking which) — 415,000/31% apart in the same payload. An earlier version of this document called this "latent... no visible total is rendered" — that was a claim about the UI, not the data, and it's weaker now that the two numbers actively disagree rather than agree. See item 22. |
| 18 | `/api/login` 500'd for **every** login | a debug `print()` left over from fix #14's refactor referenced `user_vertical`, a name that only exists inside the helper function it was extracted from — undefined in `login()`'s own scope. Live since #14 landed on `main`; the tests written for #14 called the resolver function directly and never exercised the actual route, so this shipped and stayed broken until an end-to-end manifest run (see #19) hit it. Fixed by deleting the stray print; added a static AST-based "no undefined names in this route" guard so this class of bug can't ship silently again. |
| 19 | fifth-vertical claim was previously unverified — no `manufacturing_iot` customer existed anywhere, live or local | CC-review flagged `reference_customer: null` for manufacturing_iot as worth resolving; checking the live DB directly confirmed zero customers on any vertical/customer table matched, so the earlier "went to a live customer" claim was **false as stated** (the catalog auto-discovers correctly in isolation, but nobody had actually created one). Created customer 401 ("Regression Manufacturing Co", 2 accounts, $8M ARR) via the standard manifest driver against the live EC2 server — this is what surfaced #18. `reference_customer` now resolves to it automatically (no schema/flag needed — the primary `is_reference`/`reference_for` lookup path is itself dead code, see item 21 below; the working fallback just picks the first customer row on that vertical). Independently confirmed via `tracer` (zero code-sharing with CS Pulse): pillar catalog agrees API-vs-DB (`['P1','P2','P3','P4','P5']`), row counts reconcile, no constant-ROI artifact. **The role-gate test CC-review flagged as the single most valuable remaining check** (P4 here is Safety & Compliance — the strongest test of the `pillar_roles` design, since it's the sharpest possible case: a factory-safety pillar sitting exactly where "partner" was hardcoded) — CC-review's key was scoped to customer 390 only and couldn't reach it (auth fires before the role gate, returning a scope error rather than a refusal). Run server-side instead: `partner_portal(customer_id=401, action='scorecard')` and `action='impact'` both refuse with *"vertical has no pillar in the 'partner' role..."* — the gate holds for the vertical it's never seen before, live. |

Fix 3 implements the role-gate rather than a vertical lookup table. That's the right shape — and it's now the best-tested surface in the codebase for this bug class.

---

## NEW — fixing the labels made a wrong mapping harder to see

`pillar_investments` is now correctly named. But its numbers are the six Power-of-1 metrics, re-labelled:

| pillar | impact shown | what that number actually is |
|---|---|---|
| P1 Revenue & Unit Economics | 525,000 | **NRR** Po1 |
| P2 Fleet Utilization & Goodput | 125,000 | **Product Adoption Rate** Po1 |
| P3 Reliability & SLA Delivery | 190,000 | **Ticket Resolution Time** Po1 |
| P4 **Power & Facility** | 500,000 | **Gross Revenue Retention** Po1 |
| P5 Commercial & Expansion | 100,000 | **Expansion Rate** Po1 |
| P6 Provisioning Velocity | 306,250 | **Time to First Value** Po1 |

Exact one-to-one, no overlap. So a CFO reads *"Power & Facility — $500k impact, ROI 15.1×"* and that figure is Gross Revenue Retention.

**Before the fix this was obvious** — "Channel & Partner Health" on a GPU cloud looked foreign. **Now it looks plausible**, because the pillar name is right and only the mapping underneath is wrong. Same disease, better camouflage. CC-review's framing of this, re: launch-bar item 1 below: *"a mislabel that looks plausible is harder to catch than foreign-looking data — a partner seeing power metrics would notice; a CFO reading 'Power & Facility — $500k' has no way to know it's GRR."* Keeping that distinction explicit rather than softening it away.

`POWER_OF_1_PILLAR_MAPS` needs vertical-appropriate metrics, not a re-labelling of the SaaS set. P4's lever is sellable MW and PUE, not GRR.

The double-count this section used to report (those six impacts summing to the same 1,746,250 `layered_story` reported as Growth) is now **fixed** — see item 17 above. What's left is not a smaller, quieter version of the same pattern — it's a live divergence (item 22): `pillar_investments` still sums to the old 1,746,250 while everything else says 1,331,250. Two numbers disagreeing in one payload is a harder failure to spot than one number being uniformly wrong, not an easier one.

---

## Still open, ranked

Items 5, 7, 8, 10, 11, 12, 13 re-verified twice: once by direct grep during this afternoon's pass, once independently by CC-review against the deployed build at 15:24 — both checks agree these are unchanged. No more "not re-verified" hedge needed. Item 9 is fixed (now item 17, above). Three items are decisions rather than bugs (20, 22 — both now `xfail(strict=True)`-marked, same mechanism, not "left red on purpose" prose) and one is an action nobody had proposed yet (21's swallowed-exception sweep).

| # | finding | status |
|---|---|---|
| 5 | `POWER_OF_1_PILLAR_MAPS` maps SaaS metrics onto datacenter pillars | unchanged, re-verified |
| 6 | `roi_pct` identical on all 12 accounts, a fixed ARR multiple | **FIXED + deployed + tracer-verified (`378dcadb5` on `main`, 2026-08-24).** Root cause: the benchmark branch of `_build_cfo_account_details` scaled BOTH investment and impact by the same `arr_share`, so it cancelled in the ROI ratio and every account reported the portfolio ROI (302). Fix: investment stays ∝ ARR (servicing cost ≈ account size); impact is allocated ∝ recoverable revenue = ARR × churn_pct(health) — the sanctioned 40/20/5 band, now single-sourced in `context_graph.churn_pct_for_health` — renormalized so per-account impact still sums to `total_impact`. Live on 390: roi_pct now **781 (critical) / 340 (at-risk) / 10 (healthy)**, 3 distinct values across 12 accounts; portfolio aggregate still reconciles to 302/4.0×. Tracer `constant_detector_roi` verdict=MATCH. Test: `test_cfo_account_roi_varies.py`. |
| 7 | Six NRR values in one payload | unchanged, re-verified — historical 100 · predictor **null** · Po1 baseline 105 · `nrr_current` 106 · with-intervention 114.9 · Wizard B 100. **Mechanism for Wizard B's flat 100 found 2026-08-27**, and it changes the framing: `wizard_b_pattern_db.py` → `PatternAnalyzer.from_database()` reads `journey_data` (Wizard A health-trajectory patterns) only — zero references anywhere to `PlaybookExecutionV2`/`proof_data`. It is not "no realized outcomes yet" (data-absence), it is **architecturally disconnected from realized-outcome data by construction** — confirmed live on customer 411, which now has real `proof_data` (6/15 executions resolved, real `revenue_protected`) and Wizard B is still flat 100. Whether the flat result is itself a *correct* pattern-analysis finding on 411's `journey_data`, separately from the architectural question, hasn't been checked. |
| 8 | Six ROI values | **improved as a side effect of item 17, not independently fixed** — CC-review's re-check found the six collapsed to effectively four: `roi_pct` (302), per-account `roi_pct` (302, same number now — item 6's constant), and `roi_multiple` (4) are the same claim in three units; `blended_roi` (6.3), `nrr_waterfall.roi_x` (138.6), and `realized_roi` (0) remain unreconciled. Better, still not one number. |
| 10 | `predictor_v3_portfolio_nrr: null` | unchanged, re-verified — 12/12 fail |
| 11 | Po1 metric set is SaaS-shaped | unchanged, re-verified — no GPU utilisation, goodput, PUE, commit ramp |
| 12 | `automation_rate: 0`, `time_saved_hours: 0` everywhere | unchanged, re-verified. **Exact location pinned 2026-08-27**: `utils/cfo_dashboard_helpers.py:132`, inside `build_cfo_efficiency_metrics()`'s `source: 'csPulseProof'` branch (the one that fires when real proof data exists — confirmed live on customer 411, which has real `proof_data` and does take this branch). `automation_rate` is a bare hardcoded `0` in that branch while `time_saved_hours` (line 133) and `rev_per_cs_dollar` are genuinely computed from `proof_data` in the same block — an isolated gap, not a systemic one. The `source: 'benchmark'` branch below (line ~159) already computes a real `automation_rate` from cost-bridge data; the proof branch has no equivalent input to compute an analogous number from (there's no "was this playbook run automated" signal in `proof_data` today) — fixing this needs either a new proof_data field or an explicit `null`/`not_applicable` instead of a misleading `0`. |
| 13 | `cost_of_inaction` churn_pct banded (45% for both health 14.7 and 19.8) | unchanged, re-verified |
| 20 | **DECIDED + RESOLVED (owner, 2026-08-24; `378dcadb5`)** — keep the config's per-tier investment scaling (95K/247K/370K); update the deck | Owner ruling: the flat-$247K thesis is the *less* accurate model, not the more convenient one — a $5M-ARR and a $50M-ARR CS program genuinely don't cost the same, and the product's own data contradicts a single constant (390 real `cs_investment` = $331K, 392 = $600K, 398 = $600K; investment doesn't track ARR linearly *or* fit one number even in the data already pulled). So the config stays; the **deck** is updated to present $247K as the mid-tier reference ("typical program cost; scales $95K–$370K by portfolio size"), not a flat commitment — a wording fix cheaper than bending the config, and it avoids a customer later asking "why did I pay $370K when your deck said $247K." Code: `TestNonLinearScaling` → `TestPerTierInvestmentScaling`, asserting the per-tier model (ROI 3.23/5.50/5.51, each reconciling to `total_impact/investment − 1`) and that ROI is **not** super-linear at constant cost; the 4 `xfail(strict=True)` markers are gone. *(Deck doc itself is external — the wording change is owner's to make in the sales collateral; the code no longer encodes the retired claim.)* |
| 21 | `list_verticals`'s `kpi_count` does **not** actually come from a second, independent source | Correction to an earlier version of this document (and to the architecture doc's framing of the round-trip test). `VerticalTemplate` — the model `list_verticals` tries to query for `kpi_count` — **does not exist** in `models.py`. The `try/except (ImportError, Exception): pass` around that query always fires, silently, and the function falls through to `utils.vertical_registry.get_kpis()` — **the exact same source** `get_kpi_catalog`'s `total_kpis` reads. So today there is only one real source, not two; they "agree" because it's the same call twice, not because two independent systems were cross-checked. This is itself an instance of the pattern this whole audit is about: a silently-swallowed exception that looks like a working fallback path but never executes. Same shape as `_reference_customer_for_vertical`'s primary `is_reference`/`reference_for` query (item 19 above) — also references columns that don't exist on `Customer`, also silently swallowed, also always falls through to a real but different code path. **Decision for the round-trip test (item e, Track B below):** the registry JSON catalog is canonical. If `VerticalTemplate` (or any DB-backed cache) is ever made real, it must be validated against the registry at write time — never treated as an independent check, and never allowed to silently diverge the way this one already could have if it had ever actually run. **Sweep EXECUTED 2026-08-22** (`b68f6607b` on `main`, deployed + tracer-verified). Method: rather than grepping exception patterns (188 bare `except:` hits, mostly legitimate), AST-diffed every `from models import X` in the backend against models.py's actual exports — the precise signature of this class. The prediction held: the two incidental finds were a class, not a coincidence. **Six more live instances found and fixed:** (1) `KpiScore`→`KPIScore` typo made `get_trailing_kpi_values_generic()` return `{}` forever via its except — the trailing-KPI fallback for 12+ MCP/Ask-AI call sites (latent today: `kpi_scores` has 0 rows platform-wide, but it could never have worked); (2) `RAGKnowledgeBase` imported from `models` instead of `rag_knowledge_schema` — a live upload-status route 500'd unguarded, and event_system's RAG status update silently no-oped on every rebuild; (3) `playbook_orchestrator` imported a `StatusUpdate` model that has never existed anywhere — crashed the whole module including the live n8n webhook-callback route; (4) `test_runner_api`'s customer-summary endpoint queried a nonexistent `PrecalculatedScore` — 500 on every call, now uses the canonical account-health service; (5)(6) two import scripts fixed. **Four dead files deleted** (user-approved): `utils/config_resolver.py`, `license_service.py`, `utils/account_config_manager.py`, `scripts/seed_tier_templates.py` — all zero-caller, all referencing phantom models, all crash-on-first-call. Both originally-found dead branches (this item's `VerticalTemplate` query, item 19's `is_reference` query) also removed, with comments pointing re-adders at the round-trip identity test. False positives correctly excluded: `agents/*` imports resolve to `agents/models.py`; `ProductCatalog` & co. are re-exported by models.py inside a try block. |
| 22 | **decision, not a fix — magnitude corrected 2026-08-22, it's worse than first recorded** — `pillar_investments`' per-pillar impact values aren't deduped by shared playbook, AND its investment denominator is a different figure than `layered_story`'s | Originally recorded as a 415,000 (31%) numerator gap. Re-verified live on 390: the **denominator disagrees too**, and by more. Three investment figures appear in one payload for the same underlying activity: `pillar_investments` sums its per-pillar `investment` rows to `effective_investment` (331,120 — real CS/playbook spend); `layered_story`'s Growth layer divides by `po1_cost` (552,309 — a *separate* benchmark figure, `sum(m.total_investment for m in POWER_OF_1_METRICS) * sqrt(total_arr / 10_000_000)`, computed from deck baselines, not this customer's actual spend); `layered_story.total_cost` (915,349, `cost1 + cost2 + po1_cost`) backs `blended_roi`, a third basis again. Implied ROI by surface: `pillar_investments` blended 1,746,250 / 331,120 = **5.27×**; `layered_story` Growth layer 1,331,250 / 552,309 = **2.4×**; top-level `roi_pct`/`roi_multiple` 1,331,250 / 331,120 = **4.02× → 302%**. A CFO who sums the pillar table against real spend gets 5.3×; the Growth card two rows down reports 2.4× on what reads as the same activity — a **2.2× spread**, not the 31% the numerator alone implies. `tests/test_pillar_investments_dedup_reconciliation.py::test_pillar_investments_sum_matches_deduped_growth_total` is committed and `xfail(strict=True)` — it currently only encodes the numerator half of this. **Deliberately not fixed tonight**, and now clearly a two-part decision, not one: (1) numerator — winner-take-all per pillar row (the losing metric's own pillar reads $0 impact, e.g. P2 Fleet Utilization & Goodput because PB-01 is now fully attributed to P6) vs. a proportional split vs. treating pillar_investments as a genuinely different lens nobody may sum as Growth; (2) denominator — `po1_cost`'s benchmark-scaled investment figure vs. `effective_investment`'s real spend are two different questions ("what would this cost at benchmark rates" vs. "what did this customer actually spend") that a single `layered_story` ROI number is currently answering as if they were one. Same shape as item 20 — someone with product authority over what a CFO ROI number is supposed to answer needs to choose both halves, then the xfail flips to a plain assert (and gets a second assertion for the denominator). |

Item 6 is the cheapest and most CFO-visible open item now that 9 is fixed. Item 5 remains the most misleading — it looks plausible, unlike a foreign-looking value.

| 23 | **NUMBERED WORK ITEM — the guards-never-fired sweep** (reviewer escalation, 2026-08-24; expected to out-yield 2a) | Four confirmed members of the class, all found incidentally while looking for something else: (1) `VerticalTemplate` query — always excepts silently; (2) `is_reference`/`reference_for` — same shape; (3) `count_trustworthy_causal_edges` — read `edge.source`, an attribute edges don't have, so `dropped_synthetic` was structurally always zero (fixed `1f1916333`, first firing observed live: 0→38 on cust 390); (4) `orphan_scan` — a detector never observed detecting until planted-dirt validation (tracer `78ccadb`). The sweep: **every filter, allow/deny list, and validation branch gets a test proving it excludes something** — not that it runs, that it *fires*. Method sketch: enumerate gates (grep for `.in_(`, `is_trustworthy`, threshold comparisons, `allow`/`deny`/`TRUSTWORTHY` constants, validate_* functions), and for each write a paired test: one input that passes, one that the gate must reject — the same paired clean+dirty convention as the context-graph invariants. |
| 24 | **391 CFO revenue_at_risk exceeded total ARR — root cause found + FIXED** (reviewer-traced, 2026-08-24; `cbe928225`) | I mis-attributed this to "generator emits unscaled dollars." Reviewer found the real mechanism: **391's outcomes.csv was ingested 4× → 63 byte-identical duplicate OUTCOME rows** (391-only; 390/392/398/400/401 clean), and the CFO rollup `aggregate_revenue_across_accounts` summed them raw while account-level `get_revenue_at_risk` deduped — so `revenue_at_risk` hit $39.84M = 111% of the $35.9M ARR. **Two fixes shipped:** `_dedupe_exact_outcome_nodes` in both rollups (belt — a literal duplicate can't be double-counted at any surface), and a write-time existence guard in the process_data outcome loader (suspenders — the raw INSERT had no idempotency and `source_event_id` is degenerate). 391's 63 dup rows deleted (backup on EBS+local). Live post-fix: 391 revenue_at_risk 111%→28% of ARR; 390 21%, 392 28%. **Residual — tracer probe SHIPPED (2026-08-24, `ad4c760` in `~/tracer`):** `outcome_revenue_plausibility` flags any account whose de-duplicated at_risk/lost OUTCOME sum exceeds `k×ARR` (default 1.5, `expectations.max_outcome_to_arr_ratio`). Caught its own guards-never-fired bug before shipping: the first draft filtered `revenue_impact_type IN ('at_risk','lost')` — those are CS Pulse's own *computed* bucket labels (item 26's classifier), never the literal column value (which stores subtypes: `revenue_at_risk`, `churn_risk`, `partial_recovery`, `churn_lost`, ...) — so it matched 0 rows on every tenant. Fixed to mirror the real subtype vocabulary; live on 390 now genuinely checks 7 accounts, 0 implausible. Planted-dirt validated (4/4). Still open: this is ingest-time validation, not yet wired as a blocking gate at CSV upload — remains a Track D input for the generator refactor (does the generator itself emit ARR-scaled dollars, independent of what tracer catches after the fact). |
| 25 | **SHIPPED — no longer pending (`8eb1a4541` on `main`, 2026-08-27).** Superseded the 2a-N1 waiver below — the actual fix landed directly. | `_get_po1_benchmark_metrics()` now promotes a Po1 metric to DERIVED only when the customer's context graph has OBSERVED-tier evidence tagged with that metric's pillar (new read-side helper `utils.context_graph.get_pillars_with_observed_evidence()`); synthetic/load-driver tenants (`Customer.data_origin='synthetic_eval_profile'`) always stay BENCHMARK. **Caveat, confirmed before merge, not discovered after:** no writer anywhere stamps `properties['pillar_code']` on a `ContextNode`/`ContextEdge` yet, so on live data every customer — real or synthetic — still shows BENCHMARK today. The machinery is real and tested (34/34 passing); it activates the first time some writer starts tagging `pillar_code`. Old framing ("$203.7M / 257 csv OUTCOME nodes keep false observed/tier-1/confidence-1.00") is superseded by item 30 below, which fixes exactly this for the outcomes.csv path specifically. |
| 26 | **FIXED + deployed + tracer-verified — Model C shipped (`581be26ba` on `main`, 2026-08-24)** | Was: two hand-maintained bucket maps in `utils/context_graph.py` disagreed — the per-account path filed risk-named subtypes (`revenue_at_risk`/`churn_risk`/`renewal_uncertainty`/`capacity_constraint`) under `lost`, the cross-account path under `at_risk`, so the same dollars read "Confirmed Risk" on the CFO screen and `lost` on the account drilldown. Owner signed off Model C (`ws2/item26_two_models_decision.md`, answers 1/2/3 = yes/at_risk/keep). Shipped: `_OUTCOME_RISK_TYPES` split into `_OUTCOME_AT_RISK_TYPES` (risk trajectory incl. `partial_recovery`) and `_OUTCOME_LOST_TYPES` (`churn_lost`/`contraction`/`revenue_lost`); both paths now bucket through the shared `_outcome_revenue_bucket_and_amount`; account-level `at_risk` is node-evidenced (matches CFO), the health heuristic moved to `modeled_churn_exposure`, `lost` holds realized losses only (~$0 on mid-story demo tenants, correct). **Live proof:** account-summed `at_risk` == CFO `revenue_at_risk` to the dollar on all 6 populated tenants (390/391/392/398/400/401); Titan 3535 shows at_risk $5.33M (node) / modeled_churn_exposure $3.28M (health) / lost $0; tracer 4/4 MATCH. Tests: `test_revenue_model_c.py`. |
| 27 | **FIXED — honest `outcome_node_count`, shipped with 26 (`581be26ba`)** | Was: `get_revenue_at_risk.node_count` = `len(outcome_nodes) + (1 if at_risk>0)` over a query that didn't filter null `revenue_impact` (7 reported vs 5 revenue-bearing on 3535). Now emits an honest `outcome_node_count` (revenue-bearing only) passed through `context_graph_api.py`; the load-bearing `node_count` +1 gate is kept but keyed off `modeled_churn_exposure`. Also shipped alongside: `_dedupe_exact_outcome_nodes` in both rollups + write-time duplicate guard in the outcomes loader (belt+suspenders for item 24's duplication class). |

---

## Live work — four tracks, reassessed 2026-08-22

**A · Provenance rendering** — *display treatment shipped + both named composite sites wired; TS-type flip and remaining-marker sweep still ahead*

Display treatment approved and built: `ProvenanceTierBadge` (`kpi-dashboard/src/components/shared/ProvenanceTierBadge.tsx`) — `measured` renders unmarked (a pill is the exception, not the norm, which is what makes it legible without hovering), `benchmark` gets amber specifically since it's the tier that caused the actual audit findings, `unavailable` is red and must never stand in for a fabricated `0`. Replaced two prior partial, inconsistent attempts (`ROIEngineView`'s single-fixed-color pill, `CFODashboard`'s plain-italic `SourceLabel`), keeping `SourceLabel`'s system-of-record sentences as hover detail. Verified against real EC2 data (customer 401 in-browser), which also caught the CFO cards being dark-themed — badge switched to its dark variant there.

**Composite sites wired (`2ce6a1945`, deployed + live-verified):** `_build_layered_story` now computes per-layer `data_source = most_conservative(value_tier, cost_tier)` plus a blended-totals tier over all six inputs; `nrr_waterfall` declares `most_conservative([derived, benchmark])`. The algebra produced a substantive correction, not just labels: the "Still Protectable" layer is **benchmark**, not derived — its value is real health/ARR through the churn model, but its cost is the fixed 4560-per-account cost-bridge constant, and a ROI is only as grounded as the weaker of numerator and denominator. Finding 5's notorious `roi_x: 138.6` now ships with an honest benchmark marker. Wiring also surfaced and fixed **two hardcoded frontend mislabels**: the Still-Protectable layer claimed Wizard B as its source (it's the health-churn model), and the Revenue Waterfall panel claimed "playbook executions"/measured (it's entirely modeled). Same session pattern as ever: instrumenting a surface honestly is what exposes the labels that were already wrong. Guard test: `tests/test_composite_value_provenance.py`.

**Per-metric markers now rendered (`3a605dd0f`, deployed + live-verified):** `outcome_roi_engine`'s per-metric `data_source` (sent since Aug 2026, silently dropped by the frontend's undeclared types) and the CFO path's `estimated` flag now flow through typed interfaces into `PowerOf1Cards` badges. The CFO ROI-snapshot paths got explicit tiers, and the algebra bit again: Format B snapshot cards synthesize their "baseline → current" from `default_baselines` constants while the impact is snapshot-derived — the blend is **default**, and live on customer 401 the Po1 cards now visibly say so. A fabricated baseline presented as this customer's own number was exactly the launch-bar item-2 failure mode; it's now marked at the exact surface where it renders.

**"Confirmed" overclaim retired across exec surfaces (`da7f6ea88`, deployed + live-verified, 2026-08-25):** `Confirmed Risk (Context Graph)` → `Risk Exposure (Context Graph)`; `Revenue Protected` → `Customer-Reported Saves (Unverified)` where it's genuinely the context-graph OUTCOME rollup (customer-CSV, no audit trail) — but the SAME label string on `OutcomeROIDashboard`/`ROIEngineView`/Ask-AI's historical-ROI card turned out to be a **different** number (`outcome_roi_engine.calculate_historical_roi`, real measured KPI actuals run through the Po1 benchmark rate) and got `Revenue Protected (Modeled from KPI Actuals)` instead — conflating the two would have been a fresh instance of exactly the bug being fixed. `Expansion Pipeline (confirmed)` → `Expansion Pipeline` (same unverified provenance, not previously flagged — found live sitting between the two just-fixed siblings). CFO `Portfolio ROI` on the benchmark/estimated path only (never the real playbook-proof path) → `Modeled Return (Benchmark Rates)`. CEO `Top 3 Strategic Moves` tile: dropped the false "Move every KPI {X}% this quarter" uniformity claim (each of the 6 Po1 metrics has its own `one_pct_move`, not a shared X%) + added a formula-disclosure footnote. Live verification (local frontend proxied at the EC2 backend, magic-link login) caught a duplication grep alone would have missed: the same 3-card "Confirmed revenue at risk / protected / expansion pipeline (confirmed)" block was copy-pasted near-verbatim across `CROOverviewHonesty.tsx`, `VPCSOverviewHonesty.tsx`, and inlined again in `CFODashboard.tsx` — all three fixed identically. **Bonus, same verification pass:** found and fixed two live-crashing `Decimal`/`float` bugs (`power_of_1_model.dedupe_portfolio_dollar_impact`, `ceo_dashboard`'s uncoerced `total_arr`) that 500'd the entire CEO dashboard for customer 390 — CFO's equivalent code already coerced via `_safe_float()`, CEO's never did.

→ **Next:** the remaining TS-type flip (rest of `CFOData`'s ~120 numeric fields have no tier types yet) and a similar live-browser sweep of any dashboards not yet opened this session (VP-CS view opened via source read only, not browser-verified).

**B · Vertical registry** — *DONE — correctness fix + both guard tests shipped, independently confirmed, deployed*

Checked the architecture doc's six items against actual code, not design intent. CC-review independently verified rows 1–3 live (see items 2, 3, 19 above) and took the code-level rows (4–6) on this document's report, flagging that as the boundary of what it could check from the API alone.

| item | status | evidence |
|---|---|---|
| (a) fail-closed registry, no dc2_s fallback | **done** | `get_vertical_for_customer()`, `_load_catalog()`, `_resolve_customer_vertical()` all raise on a miss — no silent substitution. CC-review confirmed live: `get_vertical_config('not_a_real_vertical')` → clean error naming the supported set. |
| (b) `pillar_roles` registry per vertical | **done** | all 5 catalogs declare it; `role()` returns `None` rather than guessing; `partner_portal` consumes it and refuses when null |
| (c) `get_kpi_catalog` if/else bypass deleted | **done** | replaced with a registry call; raises rather than falls through |
| (d) no cross-vertical imports, guarded by test | **partial** | test exists but is a ratchet (67 known sites baselined, not forbidden) — paused deliberately 2026-08-21 as backlog, not incompleteness |
| (e) round-trip identity test (pillar names match `list_verticals`, `total_kpis` matches `kpi_count`) | **done** | `tests/test_kpi_count_round_trip_identity.py` — pins both surfaces' KPI-count computation to `utils.vertical_registry.get_kpis()` as the canonical source (item 21's decision), for every registered vertical, not just to each other. Written with the correction folded in: this is a tripwire against future drift, not evidence today's trivial agreement was ever cross-checked. |
| (f) no-silent-substitution guard (returned config's slug must match the requested slug) | **done** | `tests/test_catalog_no_silent_substitution.py` + a real production fix: `load_catalog_from_json()` now takes `expected_vertical` and raises if the JSON's own `vertical` field disagrees. `healthcare_provider` and `manufacturing_iot`'s catalogs didn't declare that field at all — added, so the guard covers all 5 verticals, not 3. `_load_from_json_catalog()` no longer swallows the resulting error in a try/except — a mismatch now propagates and raises instead of silently falling through to the legacy Python-module tier. |

The refactor sequence in `vertical-registry-architecture.md` is now fully complete — steps 1-6 all done and tested. (d) remains the one deliberately-paused item (backlog, not incompleteness). Verified live on EC2 post-deploy: all 5 verticals still load correctly, `/api/login` still 200s, `tracer` 3/3 probes MATCH, diff vs. prior run IDENTICAL.

**Fifth vertical proof point, real, not just catalog-level:** `manufacturing_iot` (5 pillars, 11 KPIs, no partner/adoption roles) is a live customer — id 401, "Regression Manufacturing Co", created via the standard manifest driver against the live server, 2 accounts, health scores computed (avg 59.4), independently confirmed via `tracer`. This *is* the strongest evidence in this document for the registry design working: `manufacturing_iot`'s P4 is Safety & Compliance — exactly the shape (a factory-safety pillar sitting where "partner" used to be hardcoded) that would have handed a partner a safety-incident rate under the old code. It now refuses, live, for a vertical that didn't exist when that refusal was written.

The customer-creation attempt is also what surfaced item 18 (the login 500) — an end-to-end live create+login is a different kind of check than calling the resolver function directly, and it caught something the direct-call unit tests couldn't.

→ **Track B is closed.** Nothing left on this board except (d)'s deliberately-deferred cleanup.

**C · Edge provenance** — *WS-1 COMPLETE; WS-2 (2a through 2g) ALL SHIPPED + DEPLOYED as of 2026-08-27 — this track is done*

All WS-1 exit criteria met, deliverables in `03-edge-provenance/ws1/`:
- **Step 0** corrected a plan premise: `Account.arc_confidence` (:82) is fed by `classify_arc()` (the 8-arc cascade), NOT by `_classify_trajectory_with_confidence` (:240) — two different classifiers; the :240 value fans into **two** places (node properties :343, edge confidence :363), not three.
- **1.1** — wizard_a's untagged TRIGGERED writer (the 724-NULL-row source) routed through `upsert_edge` with structured derivation + `confidence_semantics: trajectory_rule_match_score`; AST regression guard (`tests/test_wizard_a_edge_provenance.py`). Live bonus: re-running the wizard on customer 401 **self-healed** its 2 pre-fix NULL edges (stale-node cleanup cascades them away) — forward-only converges without any backfill for re-run tenants.
- **1.2** — scope decision written (`ws1/scope_decision.md`): the two non-edge carriers DEFERRED to WS-2 per the plan's default, with the correction above recorded.
- **1.3** — **CLEARED, stop condition NOT triggered** (`ws1/roi_leak.md`): both dollar-math sites multiply OUTCOME-node confidence only; arc_detection nodes are SIGNALs with no `revenue_impact`; Wizard B uses edge confidence as a count-stat threshold only. Two carry-forwards logged: the pre-clamp >1.0 era remains a WS-4 disclosure question, and push_intelligence *spends* real playbook dollars on arc_confidence thresholds (decisioning, not multiplication — flagged for WS-2's adjudication discussion).
- **1.4** — full write-path inventory (`ws1/edge_write_paths.md`, 16 paths): found **two MORE untagged writers** the plan didn't know about (both stakeholder→decision INVOLVES linkers, both also writing a typed 0.8 heuristic constant as confidence) — fixed the same way. Also flagged: `utils/context_graph.py:828 add_edge()`, a second sanctioned-looking constructor that skips dedup AND the invariant gate — WS-2 2c should delete or fold it.
- **1.5** — every LLM-written node/edge now carries `model_id`, `prompt_version` (content-sha of the active prompt, can't drift), `inferred_at`, and `input_refs` on explicit edges. Forward-only; the ~4,663 legacy rows stay unretrofittable by design.
- Bonus: `test_provenance_writers`' vocabulary guard — failing at HEAD since it was written — is green for the first time (3 `source='system'` playbook-outcome sites → `'inferred'` per the legacy alias, 1 `'account_details'` → `'observed'`).

**WS-2, all sub-items, shipped 2026-08-27** (`docs/WS2_2F_2G_SCOPING.md` in-repo has the full engineering detail; this is the summary):

- **Adjudication matrix**: fully SIGNED (v3/v4, 2026-08-24) — not "gated on human review" as this doc previously said. Only Hold 2 (cells 1-3, csv_import's `asserted` tier) was genuinely still open, and that shipped too (below).
- **2a (schema)** + **2c (EdgeFactory)**: shipped earlier, `Customer.data_origin` (customer-level, NOT `ContextNode`/`ContextEdge` — a citation error in an earlier draft of the 2f/2g scoping doc, corrected).
- **Hold 2 (csv_import `unknown` tier)** — `5c3c5a935`. Both csv_import edge writers stamp `evidence_tier='unknown'` on new LED_TO/TRIGGERED/CAUSED_BY edges.
- **2f, node-side** — `fe1e56efd`. Root cause of the csv_import OUTCOME blind spot: `outcomes.csv`'s writer in `_process_data_impl` bypassed `upsert_node()` (the sanctioned write path that calls I3') via a raw `ContextNode()` constructor — the same bug class as `add_edge()`'s bypass, just on nodes, never previously caught. Fixed by calling the clamp directly (NOT by rerouting through `upsert_node()`, which would have reopened a 2026-08-24 dedup fix in a worse, data-loss form — `source_event_id` is deliberately degenerate for this writer).
- **2f, edge-side (I3'-E)** — `7baa59442`. Extends the clamp to causal edges. Ships in **shadow mode** (`EDGE_CLAMP_ENFORCE = False` in `context_graph_invariants.py`) — stamps `properties['would_be_clamped']`, never mutates real confidence, given the blast radius (see below). Decided: an edge with NO `evidence_tier` key at all is in-scope for the clamp, same as an explicit `inferred`/`unknown` tag — confirmed ~100% of the live edge population has no key at all, so exempting absence would make the check a near-no-op.
- **2g (supersession)** — `239823e94`. New `ContextEdge.superseded_by` column (additive, no backfill). Full decided rule: monotonic `observed > asserted > inferred > unknown` cross-tier; within `inferred`, an explicit versioned writer-priority list (`llm_enrichment > wizard_a`, in `utils/supersession.py`) rather than confidence or recency; same-writer re-fires resolved by recency; `observed`/`asserted` ties never auto-resolved (surfaced to a human, not silently picked). `get_causal_chain()` now filters `superseded_by IS NULL`.
- **Writer-tagging follow-up** — `b57f5fc36`. `wizard_a`, `tier1_inference` (llm_enrichment + llm_inference, 6 call sites), `signal_analyst`, `urgent_signal_scanner` now stamp `evidence_tier='inferred'` explicitly. **Does not reduce the shadow-mode clamp rate** — confirmed directly, an explicit `evidence_tier='inferred'` clamps identically to an absent key, since the clamp checks evidence *content*, not tier presence.

→ **What's actually left on Track C** (see item 31 below for the live data behind this): (1) `EDGE_CLAMP_ENFORCE` flip is blocked on a full audit of every `ContextEdge.confidence` consumer for a hardcoded threshold — two found and confirmed so far (`urgent_signal_scanner.py` @ 0.85, `wizard_b_pattern_db.py`'s `causal_evidence` count @ 0.6), both would likely go from normal operation to near-zero/dark overnight, not degrade gradually; (2) item 25's `pillar_code` tagging follow-up (no writer stamps it yet, so item 25's promotion logic stays dormant); (3) the standing scope cut (backfill removed, tenant data disposable) still holds — nothing above touches existing rows.

**D · Generator + audit harness** — *harness built and proving itself; generator untouched*

The two halves really were independent, as the doc predicted. Harness (`tracer`, external repo):
- `pytest`: 20 passed, 3 skipped (need a live DB tunnel)
- AT-1 import independence, AT-2 read-only enforcement, AT-3 `locked_at < fetched_at` schema constraint — all have passing tests
- AT-4 (catalog three-source mismatch) and AT-5 (constant-detector — a generic "same value across accounts of different ARR" check, illustrated by `roi_pct: 427`, now 302 post item-17-fix; the probe isn't hardcoded to either number) — the two criteria that prove it finds real bugs — both fired against live customer data (393, then independently re-confirmed against 401)
- AT-6 (`tracer diff` on two same-build runs → zero changes) — confirmed live, twice
- Not customer-390-hardcoded — target tenant is config-driven ("highest customer_id with vertical != dc2_s and > 0 accounts"), confirmed by grep and by watching the target auto-shift from 393 to 401 the moment 401 existed
- One deliberate spec deviation: read-only enforced via a connection-level flag, not a dedicated DB user — production-caution call, not an oversight
- Only 3 of the checklist's probes are built (catalog three-source, constant-detector, row-count reconciliation) — matches the doc's own "start with three, not a hundred"

Generator refactor (eval profile, `ground_truth.json`, breaking the `arc_types`-as-input circularity) is genuinely untouched — every manifest run this session, including the one that created customer 401, used the existing demo-profile generator unchanged, and the safety-property step (golden CSVs, byte-identical test) hasn't been done.

→ **Next:** the generator refactor is the only unstarted piece of Track D now, still gated on its own precondition — confirm the generator is deterministic today before archiving goldens.

**Revised order:** B's two guard tests first — cheapest available fix, closes the launch-bar's conformance-test gap directly. Then A. Then D's generator refactor. C is still independent and still unblocked. CC-review, reviewing this resequencing independently: *"sound, and I'd adopt it... the old ordering had Track B last on the assumption it hadn't started."*

---

## Newly surfaced — not yet ticketed (recorded 2026-08-24)

Un-ticketed issues found in passing, kept here so they don't evaporate. None are in-flight.

| # | issue | source | risk to fix |
|---|---|---|---|
| N1 | **`urgent_signal_scanner` dedup never fired — FIXED + deployed + verified (`5c3e58eb5` on `main`, 2026-08-24)** — reader filtered `source == 'system'`, writer wrote `source='inferred'`, zero code ever wrote `'system'`. Both routed through one `_ALERT_NODE_SOURCE` constant so they can't drift apart again. Was latent (0 `urgent_alert` nodes exist platform-wide — no demo tenant has tripped the urgency threshold yet) but structurally guaranteed to spam duplicate critical notifications once one did. Static parity guard (`test_urgent_alert_dedup_parity.py`, 3/3 pass) + functional double-scan proof on all 390 accounts (2nd run creates 0 new nodes everywhere). | item 23 sweep, CONFIRMED + FIXED | done |
| N2 | **Wizard C revenue-at-risk raw source filter** — `wizards/wizard_c_weight_calibrator_db.py:665` uses `ContextNode.source.in_(TRUSTWORTHY_SOURCES)` (`observed`,`inferred`) with **no `normalize()`**, unlike the live readers (`tier1_inference:58`, `journey_intelligence_api:521/648`, `signal_analyst:684`) which use `.in_(['observed','customer'])`. Any OUTCOME node with the legacy `'customer'` DB default (`models.py:740`) or a non-canonical real source (`action_economics`/`benchmark_fallback`, `outcome_roi_engine.py:149,262`) is silently dropped → Wizard C's NRR warning can under-fire. Lower confidence than N1 (most OUTCOME writers set canonical sources). Fix: route through `is_trustworthy`/`normalize` or add `'customer'`. | item 23 sweep, SUSPECT | LOW code, needs deploy+verify |
| N3 | **`apply_node_source_filter` is dead + mis-vocabularied** (`utils/provenance.py:116`) — no live caller (only `tests/test_provenance.py` + its own docstring; grep-confirmed). Its raw allowlist `('observed','inferred')` omits the legacy `'customer'` default, so if ever adopted as-is it would fail-closed and drop customer-default rows — the inverse of phase-1's fail-open bug, same class. **Recommend DELETE** (with the misleading `provenance.py:105` docstring cross-ref). | item 23 sweep | LOW (delete dead code) |
| N4 | **Item 22 follow-on: realized-ROI 4th bucket** — today `proof_data.realized_roi`, `revenue_protected`, and every execution `roi_x` are 0 (no real outcomes tracked yet). When outcomes get tracked, that must become a **separate, clearly-labeled** bucket ("Realized this quarter: real $ ÷ real $"), never blended into the modeled 4.0×, or it recreates the item-22 conflation. Not blocking; ticket so it doesn't quietly regress. | owner, item 22 decision | future work |
| N5 | **`get_integration_health` raw DB error** — `integration_connectors` table missing (missing migration); tool throws a raw DB error instead of degrading. | earlier session | MED (schema/migration) |
| N6 | **Arg-validation-before-auth ordering** on account-level MCP tools — argument validation runs before the auth/scope check, leaking which args are valid to unauthenticated callers. | earlier session | MED (security-sensitive, order carefully) |
| N7 | **Rename `cost_of_inaction.arr_at_risk`** — candidate rename for clarity (mechanical, pair with a frontend key update). | earlier session | LOW-MED (cross-surface rename) |

The **7 paired guard tests** for item 23's named targets (ConfigValidator, validate_arc, taxonomy `_ALLOWED_TOP_KEYS`, both `_check_prerequisites`, `_check_mcp_enabled`, the source allowlists) **shipped** (`641beb8aa` on `main`, 2026-08-24) — 35 tests, all pass in-container; every gate proven to fire on real dirty input, not just run. `apply_node_source_filter` (N3) adopt-vs-delete recommendation is DELETE (dead + vocabulary-mismatched); not yet deleted. N2 (Wizard C raw source filter) not yet fixed — both remain open, lower urgency than N1 (no confirmed live impact yet vs. N1's confirmed structural bug).

---

## 2026-08-27 additions — items 28-31

Found and/or fixed during a live EC2 verification pass on two fresh tenants (customer 411, dc2_s; customer 412, datacenter_v1) plus direct DB/tracer checks against the deployed build (`6f35debe8` → `b57f5fc36` over the course of the day). Full detail in this session's memory; summarized here so this doc stays the single re-entry point.

| # | finding | status |
|---|---|---|
| 28 | **FIXED + deployed + live-verified (`a8be6fbda`, 2026-08-28).** `accounts.account_status` went stale after a health recompute — the 2026-04-20 fix for exactly this symptom lives in `utils/score_calculator.py`'s `_save_scores()`, but that function isn't what runs during normal onboarding: the real pipeline (`process_data_pipeline.py`'s `calculate_health_scores()`) writes `health_scores` via a raw engine connection, bypassing the ORM `Account` model entirely, so `account_status` never got touched by it. Reproduced live on 415 (Phoenix Hyperscale, Atlas Cloud Services: `account_status='at_risk'` while latest `health_status='healthy'`). **Risk evaluated before fixing**: traced every real consumer of `account_status` — `analytics_api.py`/`unified_query_api.py` do exact-match filtering (a CRO/CSM filtering to "at-risk accounts" would get false positives on recovered accounts), `predictor_api.py`'s eligibility filter and `context_graph_api.py`'s churned-check are both unaffected by this specific staleness direction. **Separate, adjacent finding surfaced during the risk pass, not fixed here**: `playbook_triggers_api.py:261` and `health_trend_api.py:314` both gate on `account_status == 'At Risk'` (title case, space) — grepped the whole codebase, that exact string is written nowhere except a test fixture; every real writer stores lowercase `at_risk`/`active`/`churned`. Those two branches are permanently dead in production regardless of this fix — a string-format mismatch on the reader side, same "guard never fires" class as items 21/23, logged separately rather than folded in. **Fix**: mirrors `score_calculator.py`'s proven 3-rule mapping (healthy→active, at_risk/critical→at_risk, churned left alone) for exactly the accounts whose health was recalculated this run (`changed_account_ids`, already computed), applied right after the raw-SQL health-score write commits. **Live proof**: fresh customer 416 (post-deploy registration, same manifest/account shape as 415) shows zero mismatches across all 10 accounts — the two accounts that reproduced the bug on 415 (Phoenix Hyperscale, Atlas Cloud Services) now correctly show `account_status='active'` matching `health_status='healthy'`. | FIXED |
| 29 | **Load-driver's manifest-mode registration never stamps `Customer.data_origin`.** `cs_pulse_driver.py` has two registration call sites: `--manifest --register` (line ~139, the default/standard path, used for virtually all tenant creation including 411/412) never passes `data_origin`; `--profile eval --world-id` (line ~432) does, `'synthetic_eval_profile'`. Confirmed live: both 411 and 412 have `data_origin=NULL`. Item 25's `_is_synthetic_customer()` gate only recognizes `'synthetic_eval_profile'`, so the much larger manifest-mode tenant population is indistinguishable from a real customer signup for that purpose. Needs a product decision (new `data_origin` value for manifest mode, or intentional as-is) — not touched. | confirmed, undecided |
| 30 | **FIXED — load-driver's `outcomes.csv` generator never populated `evidence` (`0fdf27aeb`).** Consequence of item 25/2f shipping: any OUTCOME node with no evidence content now gets I3'-clamped to confidence=0.3, below `get_revenue_at_risk()`'s 0.5 filter — and `scenarios/scenario_manifest.py`'s `generate_outcomes_csv()` (the primary generator, used by every `cs_pulse_driver.py --register` tenant) never wrote that column at all. Would have silently zeroed revenue-at-risk/-protected on every future load-driver tenant. Fixed by reusing each outcome's already-generated narrative description as its evidence text (not fabricated). Verified live, cross-vertical, on both 411 (dc2_s) and 412 (datacenter_v1): every OUTCOME node shows `confidence=1.00`, real evidence text. 3 AT-0 golden fixtures regenerated, diffed clean (zero non-evidence-column drift). | fixed + deployed + live-verified |
| 31 | **`EDGE_CLAMP_ENFORCE` pre-flip consumer audit — in progress, live data now backing it.** Two confirmed consumers of `ContextEdge.confidence` with a hardcoded threshold that a clamped edge (0.3) universally fails: `utils/urgent_signal_scanner.py:84` (`>= 0.85`) and `utils/provenance.py`'s `count_trustworthy_causal_edges()` (`>= 0.6` default), called live from `wizards/wizard_b_pattern_db.py:73` into `causal_evidence`. Live baseline pulled for the second one on customer 411 post-deploy: `{"total": 103, "trustworthy": 38, "dropped_synthetic": 65, "dropped_low_confidence": 0}` — zero currently dropped for confidence, meaning most/all of the 38 "trustworthy" edges are very plausibly the fresh `wizard_a` edges written during this same verification pass (confirmed 100% `would_be_clamped: true` on live sampling, 4/4 wizard_a edges checked). Post-flip, `trustworthy` likely crashes from 38 toward single digits/zero — not gradual degradation. No equivalent baseline pulled yet for `urgent_signal_scanner` (it has no intermediate diagnostic output the way `causal_evidence` does — needs a direct SQL count against `context_edges` joined to qualifying OUTCOME nodes, `confidence >= 0.85`). **Do not flip `EDGE_CLAMP_ENFORCE` until this sweep is complete** — full sweep for other hardcoded-threshold consumers, not just these two, is still open. |
| 32 | **FIXED (`9fc35fe6b`, 2026-08-27).** `push_intelligence_subscriber.py` fabricated `arc_confidence=0.5` for not-yet-classified accounts, feeding a live playbook auto-trigger decision — a worse shape than 28/31, not just another instance. `Account.arc_confidence` is set only by Wizard A (`wizards/wizard_a_journey_db.py:80-82`); `account_details.csv`'s loader never sets it, so every account starts `NULL`. `evaluate_playbook_trigger_for_account()` (line 285) does `arc_confidence = account.arc_confidence or 0.5` — unlike every prior finding this session, this doesn't just leave an absence visible, it **manufactures a plausible-looking real value indistinguishable from an actual 50%-confidence classification**, with no way to tag it as synthetic after the fact (compare a `benchmark`/`modeled` label, which can be attached to a modeled *number* — there's no equivalent for a fabricated *confidence* sitting in the same field as real ones). Confirmed this gates a live decision, not just display: `if health_score < 50 and arc_confidence >= 0.7: decision='auto_approved'` (line 355) — a fabricated 0.5 fails this and falls to `pending_manual_approval` with a displayed `confidence=0.4`, and the fabricated value also flows into `PlaybookTriggerValidator.validate_trigger()`'s `trigger_context` (line 343) on the normal, non-exception path, not just the fallback. **Confirmed structurally normal, not rare**: traced the actual pipeline order in `_process_data_impl` — `calculate_health_scores()` (line 2145) → `publish_health_events()` (line 2159, publishes the event this subscriber's background thread reacts to) → ... → `run_wizard_a` (line 2426, ~280 lines later). The subscriber's async evaluation starts right after publish, well before Wizard A sets the real value, on every onboarding run. Nuance: the fabricated value only reaches a real decision if `arc_type` resolves from *somewhere* (`account.arc_type` or a fallback `arc_detection` `ContextNode`, line 289-303) before Wizard A runs — if neither exists yet, the function returns `None` first (safe no-op) — so this is a real, structurally-present subset, not literally every single evaluation. `journey_intelligence_api.py:222-223` already does this correctly on the same field (`... if account.arc_confidence is not None else None`) — same fix idiom applies here. **Ranked above item 31 in urgency**: silently-wrong outranks correctly-blocked-pending-review. **Fix**: `evaluate_playbook_trigger_for_account()` now returns `None` (defers to the next health-score event) when `arc_confidence` is genuinely unset, for both the `Account.arc_confidence` path and the `ContextNode` arc_detection fallback path — same idiom `journey_intelligence_api.py` already used correctly. 4 new tests (`tests/test_arc_confidence_no_fabrication.py`), independently re-verified passing; confirmed a genuinely-classified account still proceeds to a real decision (not over-corrected into always deferring).

**Distinction worth stating precisely** (reviewer feedback, 2026-08-27): this produced fabricated data *today*, on every current run, independent of any future flag flip — a "this has been happening" bug, not a "before we change something" risk like item 31's `EDGE_CLAMP_ENFORCE` sweep. Don't file it with the same urgency-but-not-yet-live framing.

**Follow-up gap closed same day:** deferring (returning `None`) is correct, but on its own it silently drops the original triggering event rather than delaying it — confirmed nothing else re-publishes `HEALTH_SCORES_UPDATED` for that account once Wizard A actually sets the real `arc_confidence`, since that event fires exactly once per account during onboarding (`process_data_pipeline.py`'s `publish_health_events`). Added `retry_deferred_evaluation_after_arc_classification()`, called synchronously from `wizards/wizard_a_journey_db.py` right after committing the real `arc_type`/`arc_confidence` — deliberately bypasses `ArcPlaybookSubscriber`'s event/cooldown machinery rather than re-publishing the event, since the 600s cooldown gets set the moment the *original* event was received (whether or not it produced a decision) and would very likely swallow a re-published event fired seconds later in the same pipeline run. Applies the same feature-flag (`layer_b_enabled`/`auto_trigger_on_arc`) and health-classification gates the live event path uses. 4 new tests (`tests/test_arc_confidence_deferred_retry.py`): confirms a deferred evaluation gets a real decision once classification lands, healthy accounts still correctly get no decision, and the retry never raises even for a bad account_id. 24/24 assertions passing across both new test files + Wizard A's existing edge-provenance suite, no regressions. | FIXED |
| 33 | **FIXED, same day — item 30's twin, found by deliberately auditing for exactly this ("is there a third `ContextNode` writer that also bypasses the I3' clamp").** `utils/playbook_lifecycle.py`'s `_write_context_graph_outcome()` has three raw `ContextNode(node_type='OUTCOME')` constructors (lines ~469/485/504) — every playbook-close OUTCOME node (`node_subtype='playbook_outcome'`), covering revenue-protected, revenue-expanded, and no-revenue closes. None ever called `upsert_node()`, so none ever hit the I3' clamp: confirmed live on customer 408, **every existing `playbook_outcome` row shows `confidence=1.00, tier=1, evidence=NULL`** — a 100% bypass rate, unconditional, on the exact code path that produces the "measured, not modeled" proof data (`proof_data.executions_resolved`, the `layered_story` "Already Delivered" layer) surfaced earlier this session on customer 411. Unlike item 30's outcomes.csv fix, there IS a genuine non-fabricated evidentiary basis available here — the tracked `PlaybookExecutionV2` record itself (`health_at_trigger`/`health_at_close`/`health_delta`/`total_cost`) — so the fix stamps real evidence text derived from it plus a `source_ref` pointer to the execution row (`playbook_execution:<execution_id>`), and still routes through `clamp_unearned_confidence()` as defense-in-depth for the case where those health fields are missing (confirmed real: a `'timeout'` outcome can have `health_at_close=None`). **Regression caught before shipping, not after**: the evidence f-string originally used `:.1f` formatting directly on `execution.health_at_close`/`health_delta`, which the OLD plain-dict `common_props` never did — `None.__format__('.1f')` raises `TypeError`, a crash risk the old code never had. Added a `_fmt()` helper with a safe `'unknown'` fallback before this shipped. Also caught and fixed a **pre-existing test scope collision**: `tests/test_playbook_close_edge_abstention.py`'s `test_typed_confidence_literals_gone_from_close_path` scanned for *any* `confidence=` keyword in the function (not scoped to edge-construction calls), so it flagged this fix's legitimate `clamp_unearned_confidence(..., confidence=1.0, ...)` node-side call as a false-positive reappearance of the historical typed-edge-confidence constants it was actually written to guard against — narrowed to match its sibling test's scoping (`ContextEdge`/`upsert_edge`/`add_edge` calls only). 3 new tests (`tests/test_playbook_close_outcome_clamp.py`, including a dedicated regression test for the `None`-formatting crash risk); 72/73 assertions passing across the full adjacent suite, the one failure confirmed pre-existing and unrelated (the item-23/24-era live-tenant residue xfail, about edge confidence, not touched by this fix). The other two prioritized audit questions closed same day, below (item 34). | FIXED |
| 34 | **Audit questions #1/#2 closed (2026-08-27).** #2 (direct `properties[...]` bracket access without `.get()` fallback, anywhere in the codebase, not just OUTCOME-related keys) came back **completely clean** — zero hits, same result as the `qualitative_signals`/`Account.profile_metadata` audits earlier the same day. #1 (type-crossing: reading an OUTCOME-only field like `revenue_impact` off a `ContextNode` query that isn't actually scoped to `node_type='OUTCOME'`) found one real, low-severity instance: `outcome_roi_engine.py`'s `key_decisions` list (properly filtered to `node_type=='DECISION'`) unconditionally emitted `'revenue_impact': float(n.revenue_impact) if n.revenue_impact else 0` for every decision — a field that structurally doesn't apply to decisions, presented as a meaningful `$0` instead of not-applicable. **Confirmed zero financial-math impact before fixing**: `key_decisions` is assembled and returned as display data only — grepped every reference, nothing sums or aggregates it into any revenue total. Fixed anyway (one line, `else 0` → `is not None else None`) since it's the same labeling-honesty class as item 12's `automation_rate: 0` — no dedicated test added given the confirmed-zero blast radius and no existing test coverage to extend; 26 tests in the 3 adjacent `outcome_roi_engine.py` test files re-verified passing. Checked several other `revenue_impact`-reading call sites flagged by the same sweep (`context_graph_api.py`, `mcp_server/cs_pulse_intelligence.py`) — all already correctly gate on `if n.revenue_impact is not None`, no fix needed there. | FIXED |

| 35 | **SHIPPED — Journey Intelligence graph now surfaces the real evidence text items 30/32/33 stamp on ContextNodes (`085c5591a`, 2026-08-27).** Confidence/tier already reflected whether a node earned its trust; nothing rendered *why*. `journey_intelligence_api.py`'s outcome and decision layers now include `properties['evidence']`; `JourneyIntelligenceView.tsx`'s existing hover-tooltip convention (native SVG `<title>`, same as signals/playbooks) picks it up automatically. Verified live on customer 414/account 3726 (Orion Models): hovering a churn-averted marker shows "Retention plan executed. Account committed to renewal."; hovering the fresh playbook-close marker shows the real health/cost narrative. Deployed + DOM-verified (browser screenshots can't capture native tooltips, so verified via direct DOM query instead). | FIXED |
| 36 | **Dashboard label-accuracy audit — CFO/CRO/CEO/CSM, ~173 fields checked (2026-08-27).** Four parallel agents applied this doc's own bug taxonomy (items 5/6/7/8/12/17/22/34) to every visible label across the four exec/CSM dashboards. Full findings + fix status: `../06-dashboard-label-audit/findings.md`. Headline results: CFO — 7 new issues found, **all 7 fixed same day** (mislabeled ROI-scaling tile, hardcoded $235K/mo investment-ramp fallback, re-derived health-threshold widget, dead renewals-at-risk banner now wired via a shared `_compute_renewals_at_risk()` helper CRO also uses, meaningless NRR/playbook ratio removed). CRO — 12 new issues found, 0 fixed (top: sidebar mislabels a number the top tile calls "Actual" as "Estimated," same screen). CEO — 11 new issues found, 0 fixed (top: PortCo's "Synergy Realization Tracker" is a 100%-fabricated realized/projected split with a real, unused DB column sitting right there — the single worst finding of the sweep). CSM — ~20 new issues found, 0 fixed (top: both CSM layouts silently substitute fictional mock accounts/actions/**approvals** on any unexpected API shape, not just on failure — confirmed live on customer 390 right now via a `pending`-vs-`approvals` key mismatch — so Approve/Reject can be clicked against fake data with zero server-side effect). User decision: stop after CFO fixes + full list; CRO/CEO/CSM deferred to a future session. | CFO fixed, others documented+open |

**Attempting that SQL check surfaced a separate, prior finding, confirmed at full scale (not sampled): every large-negative-revenue OUTCOME node on both fresh tenants has ZERO inbound edges of any kind — 14/14 on customer 411, 14/14 on customer 412 (28/28 total, `revenue_impact < -50000`).** This means `urgent_signal_scanner`'s candidate set for this exact node type may already be structurally empty on Day-1 tenants, independent of `EDGE_CLAMP_ENFORCE` entirely. Root cause found in `scenario_manifest.py:4346-4350`'s own code comment: **by design**, `signal_edges.csv`/`decisions.csv` are deliberately NOT uploaded during standard registration — the model is a real customer's CRM wouldn't have a "signal causality" file; the platform (Wizard A, playbook cycles) is meant to generate those causal edges over time, post-onboarding. So a brand-new tenant having zero inbound edges into its CRM-sourced (`outcomes.csv`) OUTCOME nodes may be the intended cold-start shape, not a bug. The open question this raised — "does Wizard A ever connect back to these specific nodes, or are the two populations structurally parallel forever" — is now answered by item 37 below: parallel forever, and not by cold-start design.

| 37 | **`outcomes.csv` OUTCOME nodes structurally could never get an inbound causal edge — root cause confirmed 2026-08-27, split into 3 sub-items below (same convention as WS-2's 2a/2c/2f/2g breakdown, not one undifferentiated ticket). All 3 sub-items (37a/37b/37c) SHIPPED 2026-08-28.** Pre-fix: confirmed live across 7 tenants (405/406/407/408/411/412/414), all 5 `csv_import` revenue-bearing subtypes, `chain_length: 0` on every sampled node — the exact subtypes feeding the CFO dashboard's headline dollar figures. Post-fix: 100% connectivity on a fresh tenant (415), see 37a/37b below. **Two false leads corrected during the investigation, applying to all 3 sub-items below**: (a) the load-driver's generator (`scenario_manifest.py::generate_outcomes_csv`) does NOT uniformly fail to populate a linking field — 2 of its 3 writer blocks (arc-spine outcomes, lines 2612-2686; auto-recovery outcomes, 2704-2743) genuinely emit a real per-account signal reference under a column named `linked_signal_id`; only the lifecycle-events block (2762-2801) leaves it hardcoded blank. (b) A live "pull the raw outcomes.csv and check" is unreliable for this question: `download_customer_csv`'s export reconstructs the file from `ContextNode.properties.get('related_decision_id', '')` — a properties key nothing in the codebase ever writes — so it shows blank on every row regardless of what the original upload contained, including server-created `playbook_outcome` nodes that were never in an uploaded outcomes.csv at all. `related_decision_id` is also the expected column name in a second, apparently-dormant ingestion path (`onboarding_api_v2_config_aware.py`) that doesn't match the generator's `linked_signal_id` header either — three small independent mismatches stacked on one symptom. |
| 37a | **FIXED + deployed + live-verified (`5cdac57b1`, 2026-08-28).** `_process_data_impl`'s outcomes.csv loop now collects each row's `linked_signal_id`, resolves it against a SIGNAL node in the same account (by `source_event_id` match) once the flush gives every new OUTCOME node a real `node_id`, and writes a `LED_TO` edge — same `evidence_tier='unknown'` + `CSV_IMPORT_DERIVATION` convention Hold 2 already established for `signal_edges.csv`, since the platform still can't tell a load-driver upload from a human one at write time. **Live proof, fresh customer 415 ("Phoenix Data Centers," registered post-deploy via `phoenix_4phase_dc2s.json`, the one manifest with real lifecycle events)**: all 13 outcomes.csv-native OUTCOME subtypes show **100% inbound-edge connectivity (33/33 nodes)** — up from the ~95% orphan rate confirmed pre-fix across 5 tenants. Verified the connectivity comes from the new mechanism specifically (not a coincidental pre-existing edge): node-subtype counts through `derivation='csv_import.unattributed'` edges match the total node counts exactly, 1:1, for all 13 subtypes. Spot-checked one edge in full: `LED_TO, confidence=1.00, evidence_tier=unknown, from=champion_advocacy (SIGNAL) → expansion_closed (OUTCOME)` — correctly formed, and coexists cleanly with a second, independent pre-existing edge into the same node from a DECISION (`expansion_strategy`, confidence 0.80, no tier) written by some other process — multiple valid causal edges into one outcome isn't a conflict. | FIXED |
| 37b | **FIXED + deployed + live-verified, same commit/customer as 37a — owner decision (2026-08-28): compute a real reference, don't exempt.** The lifecycle-events outcome-writer block (`scenario_manifest.py:2762-2801`) now computes `lc_sig_ref` the same way `first_sig` does elsewhere in the function (first registered signal for the account, synthetic-but-consistent fallback if none), instead of hardcoding `linked_signal_id` blank. **Live proof**: customer 415's `expansion_closed` node (the one lifecycle-event subtype that actually fired in this test — see caveat below) shows full inbound connectivity via the same new edge mechanism as 37a, confirming the reference is real and resolves correctly, not just present. **New, separate finding surfaced by this test, not yet investigated**: customer 415's manifest declares a `churn` lifecycle event for "Crest Computing" (`event_month=6, delta_pct=-100`), but **no `churn_lost`/`contraction`/`new_logo`/`renewal_confirmed` OUTCOME node was created for any account** — only the one `expand` event's `expansion_closed` row appeared. This is unrelated to 37a/37b (which only affect the `linked_signal_id` value on rows that get written, not whether a row gets written at all) — looks like a separate, pre-existing gap in the lifecycle-events writer or a suppression/dedup interaction eating the churn row silently. Not filed as a numbered item yet — flagging here so it doesn't get lost. | FIXED (+ new unfiled finding above) |
| 37c | **FIXED + deployed + live-verified (`171233d3d`, 2026-08-28).** I3 (`invariant_i3_no_orphan_revenue_outcomes`) **is registered and does fire** — called after every Wizard A run (`process_data_pipeline.py`) — but `log_violations_summary()` already computed a real per-invariant count and just never had it captured; it went to a `logger.warning()` line nobody reads, no report/dashboard/alert anywhere downstream. Distinct from item 30/33's "writer bypasses the clamp" shape — this is "the check runs and correctly flags the defect, but nothing reads its output." **Fix**: `run_wizard_a_step()` now returns the summary dict; `_process_data_impl()` surfaces it as `context_graph_audit` in the process-data response. **Live proof**: customer 417's onboarding-status response shows `"context_graph_audit": {"by_invariant": {"I2": 8}, "total": 8, ...}` — a real invariant (I2, polarity consistency) genuinely caught and surfaced, not a stub or empty placeholder. | FIXED |
| 38 | **FIXED + deployed + live-verified, 2026-08-28 — but the first fix attempt was itself wrong, corrected same day.** STAKEHOLDER→DECISION `INVOLVES` linking almost never fired: surfaced re-verifying 37a on a second account (customer 415, Zenith Data Corp/3734 vs Crest Computing/3740, same tenant, same run) — Zenith's "Escalate competitive threat to VP Sales" DECISION had zero edges and all 4 of its stakeholders had zero edges, while Crest's stakeholders each had one. Confirmed at scale across 10 tenants: only 14/236 STAKEHOLDER nodes platform-wide had ever gotten any edge. **First diagnosis (partially wrong, corrected below):** found the linking block (a real, correctly-designed `ROLE_DECISION_MAP`) ran right after `stakeholders.csv` loaded but before `decisions.csv` opened 250 lines later — moved it to the end of the CSV-loading section instead. **Deployed, then live-verified as a near-total no-op**: a fresh customer (417) showed the fix produced zero new matches — the only linked stakeholders (3/36) turned out to have `derivation=''` (blank), proving they came from an unrelated mechanism (`push_intelligence_subscriber.py`'s own stakeholder linker for its auto-generated `playbook_exec_sponsor_change` decisions), not from this fix at all. **Real root cause, found by not trusting the first "fixed" result and tracing where the DECISION nodes actually come from**: standard registration (the only 4 CSVs load-driver ever uploads — account_details, kpi_measurements, qualitative_signals, outcomes) **never uploads a `decisions.csv` at all.** The DECISION nodes that exist for these customers (`escalation`, `expansion_strategy`, `renewal_strategy`, `investment_approval`, `exec_engagement`) come from Wizard A's `utils/arc_decision_generator.py` — which hadn't run yet even at the corrected first-attempt location, since that's Stage 3, after the CSV-loading section entirely. **Fix, corrected**: moved the linker again, this time to run immediately after `run_wizard_a_step()` completes, where arc-generated DECISION nodes genuinely exist. Also fixed a real bug in the first attempt before it shipped further: the except clause referenced a bare `logger` that doesn't exist at module scope in this file (would have raised `NameError` masking the real exception). **Live proof, fresh customer 418**: 5 new edges created with `derivation='process_data.stakeholder_role_match'` (this fix's own tag), matching the role-map's design exactly per account — e.g. `executive_sponsor→escalation` (3764), `champion/cs_manager→renewal_strategy` + `csm/cs_manager→playbook` (3765) — cleanly distinguishable from the 3 blank-derivation edges from the unrelated push_intelligence mechanism (3770). **Residual, not a bug**: overall connectivity is still well under 100% (2/10 champion, 2/10 cs_manager, 2/10 csm, 1/6 executive_sponsor on 418) because most decisions on this manifest are `expansion_strategy`, which matches no role in `ROLE_DECISION_MAP` at all — traced this precisely per-account and confirmed every non-match is a genuine map-coverage gap, not a mechanism failure. `ROLE_DECISION_MAP`'s narrow coverage is a legitimate follow-up (expand the map, or accept that most decisions are role-agnostic by design) — not filed as its own item yet. | FIXED (self-corrected once before landing) |
| 39 | **Item 5 scope, decided (2026-08-28): build Po1 metric substitution tables for all 5 verticals before it ships — not datacenter_v1-only.** Surfaced while scoping a drill-down implementation brief (Path A/B/C, revenue/NRR click-through): the reasoning "does this need a per-vertical prompt" was checked precisely rather than assumed, and the answer split in two. Path A/B/C (revenue drill-down via `get_revenue_at_risk`, canonical-NRR question, `get_context_graph_mermaid`) are confirmed vertical-agnostic by tracing the actual code — they operate entirely on OUTCOME/DECISION node *subtype strings* (`churn_averted`, `revenue_at_risk`, etc.), one layer above the KPI/pillar abstraction the vertical registry already generalizes; no vertical branching exists anywhere in that chain. Item 5 (`POWER_OF_1_PILLAR_MAPS` mapping SaaS metrics onto non-SaaS pillars) is the opposite case — it lives exactly at the KPI-code layer (`NRR→P1-KPI1`, `TTFV→P6-KPI1` for datacenter_v1) that Path A/B/C's answer explicitly carves out as still vertical-specific. A datacenter_v1-only substitution table doesn't generalize to dc2_s/manufacturing_iot/healthcare_provider by construction. **Owner decision**: rather than ship datacenter_v1-only with the other 4 verticals left on generic/SaaS-shaped Po1 metrics (a statable, narrower limitation), build real substitution tables for all 5 verticals before anything ships. Not yet started — needs each vertical's actual pillar/KPI catalog reviewed to produce vertical-appropriate mappings (e.g. P4's lever for datacenter_v1 is sellable MW/PUE, not GRR — the equivalent domain-meaningful substitution needs separate thought for manufacturing_iot, healthcare_provider, dc2_s; saas_premium is already correct, it's the source set item 5 found copied everywhere else). **Concurrency check done ahead of time (2026-08-28), so this doesn't need re-verifying when the work starts**: confirmed no conflict risk from provisioning tenants of different verticals concurrently. `POWER_OF_1_PILLAR_MAPS` (`outcome_roi_api.py:57`) is a static module-level dict keyed by vertical string, pure `.get(vertical)` read; the one per-request derived value (`nrr_synthesis`) is already explicitly copied before use (`dict(nrr_synthesis)  # don't mutate the module-level dict`, comment already in the code). `utils/vertical_registry.py`'s catalog caches (`_pillars_cache`/`_kpis_cache`/`_pillar_roles_cache`) use the same safe shape — keyed by vertical string, lazily populated, worst-case cold-cache race just reloads the same deterministic JSON redundantly. **Design constraint for the implementation**: add manufacturing_iot/healthcare_provider/dc2_s-refinement entries as new top-level keys in this same `POWER_OF_1_PILLAR_MAPS` dict, following the exact pattern already proven safe here — do not introduce a different, more mutable structure to hold the new per-vertical tables. **Confirmed concretely live, not just from the pillar_metric_map dict (2026-08-28)**: pulled `config/power_of_1_economics.json`'s actual `GRR` entry — the one `datacenter_v1`'s P4 ("Power & Facility") pillar maps to. `display_name: "Gross Revenue Retention"`, work packages `renewal_playbooks_by_segment`/`executive_business_reviews` — a datacenter_v1 CFO's Power & Facility improvement lever literally reads "Gross Revenue Retention" today, zero PUE/MW/thermal semantics anywhere. **Scope deepened**: the same entry has a field named `dc2s_linked_playbooks: ["PB-02", "PB-05"]` — hardcoded to dc2_s's playbook IDs regardless of which vertical is reading it. Checked both catalogs directly: dc2_s's `PB-02` is a real hardware playbook ("RMA Prevention" — thermal/component review, firmware/cooling fixes); **`datacenter_v1` has no `PB-02` in its own catalog at all**, only `PB-05`. So the Po1-card-to-playbook link is a dangling reference for datacenter_v1 today, not just a label problem — a different, narrower gap than item 41's playbook-recommendation-engine finding (that engine is correctly vertical-specific; this is one hardcoded linkage field inside the Po1 economics config, unrelated to the engine itself). Whoever builds the per-vertical substitution tables needs to also re-key `power_of_1_economics.json`'s `*_linked_playbooks` fields per vertical, not just relabel the metric names. **Responsibility split, noted 2026-08-28 for whoever picks this up**: two different kinds of work, not one. (1) Mechanical wiring — connecting `datacenter_v1/vertical_config.py`'s already-invented lever names (`reserved_commitment_coverage`, `power_passthrough_margin`, `utilization_uplift`, `goodput_sla_credit_avoided`) into `POWER_OF_1_PILLAR_MAPS`/`power_of_1_economics.json` so the CFO dashboard's P4 card shows one of these instead of "Gross Revenue Retention" — this is engineering work, the domain thinking already happened in the playbook config, it's a connection problem between two files that should agree but don't. (2) The dollar-value assumptions each new lever needs (`baseline`, `annual_impact_per_pct`, `total_investment`, `roi_at_1pct`, real work-package hours) are financial/business modeling, not derivable from code — same class of decision as item 20's per-tier investment scaling, needs real pricing/FDE authority, not an engineering guess. Do the wiring with placeholder values explicitly flagged as such if (2)'s real numbers aren't available yet — never fabricate them silently. **Where the dollar assumptions come from, checked precisely (2026-08-28) — not the load generator**: `load-driver/scenarios/scenario_roi_simulation.py` has its own `PILLAR_IMPACT` dict, hand-transcribed from `power_of_1_economics.json` with a comment saying exactly that ("from power_of_1_economics.json") — it *consumes* the already-established business assumptions to make synthetic demo outcomes internally consistent, it does not derive or generate them. The load-driver has never been the source of these numbers for any vertical, including SaaS — real business/pricing modeling established them first, the load-driver was updated afterward purely for demo-data realism. So datacenter_v1's new levers need the same order: real numbers from whoever owns pricing/FDE modeling first, load-driver update second (lower priority, cosmetic for synthetic-tenant realism only). **Fourth file needing the same update, found via this same check**: `scenario_roi_simulation.py`'s `PILLAR_IMPACT` is itself dc2_s-pillar-shaped and static (`P4: 100000 # Channel Partner → GRR`) — a fourth place (after `POWER_OF_1_PILLAR_MAPS`, `power_of_1_economics.json`, and `datacenter_v1/vertical_config.py`'s lever names) that would need updating for datacenter_v1's real numbers to show up consistently in synthetic demo data too. | scope decided, not started — concurrency pre-verified safe |
| 40 | **`CustomerConfig.vertical` had a `saas_premium` schema default acting as exactly the kind of silent fallback the fail-closed registry refactor was built to eliminate — found while answering "what's the root vertical now," fixed same day, not yet deployed.** `get_vertical_for_customer()`/`_resolve_customer_vertical()` already correctly fail closed on a genuinely-unset vertical (confirmed: "No fallback to dc2_s" is real, both raise `ValueError`/`ToolError`). But `models.py`'s `CustomerConfig.vertical = db.Column(db.String(50), default='saas_premium')` meant "unset" almost never actually happened — any `CustomerConfig` row created without an explicit `vertical=` kwarg silently got `saas_premium` instead of raising. **Confirmed live and real, not theoretical**: `registration_api.py`'s `/api/register` — the exact endpoint the load-driver's `--register` calls, and presumably the real customer self-signup path — extracts `vertical` from the request body (used correctly for `customer.vertical` and ID-prefix generation) but never passed it to its own `CustomerConfig(...)` constructor, relying entirely on the column default. Every customer registered through this path got `CustomerConfig.vertical='saas_premium'` regardless of what vertical they actually requested. **Why this didn't visibly break anything all session**: `_resolve_customer_vertical()`'s tier-1 check (`if cfg and cfg.vertical:`) treated the wrong-but-present `'saas_premium'` string as authoritative and never fell through to tier 2 (`Customer.vertical`, correctly set) — but a *later* onboarding step (`onboarding_api_v2_config_aware.py:2089`, the `configure_customer_kpis`-style handler the load-driver calls next) unconditionally overwrites `config.vertical = vertical` with the real value, silently correcting the mistake before any KPI catalog actually got served. The bug was real but had a narrow, undocumented rescue downstream — not "never fired," closer to items 30-38's "fires but nobody sees it" shape, except here it fixed itself instead of surfacing. **Fix**: (1) `models.py` — removed the `default='saas_premium'`, no schema/DDL change needed (Python-side ORM default only, column already nullable); a `CustomerConfig` created without an explicit vertical now stores `NULL` and the existing fail-closed resolvers correctly raise instead of silently mislabeling. (2) `registration_api.py` — the actual root-cause gap, now passes `vertical=vertical` explicitly at creation, matching the DB-verified-correct end state directly instead of relying on the later corrective overwrite. **Side-effect verified, not just assumed**: `customer_management_api.py`'s `POST /api/customers` had the identical gap (`vertical` resolved via `ensure_uuid()` onto `Customer.vertical` correctly, never passed to `CustomerConfig`) — traced and confirmed this is now correctly self-healing via tier-2 fallback once tier-1 is honestly `NULL` instead of wrongly `'saas_premium'`; left unchanged since the fix already fixes it. **Deployed + live-verified (2026-08-28)**: called `/api/register` directly with `vertical=manufacturing_iot` for a fresh customer (419) and checked the DB immediately — no CSV upload, no process-data, no chance for the downstream corrective step to run. `CustomerConfig.vertical='manufacturing_iot'` was correct at creation, proving the root-cause fix works directly rather than continuing to depend on the later rescue. | FIXED |
| 41 | **Two real vertical-coverage gaps found while checking "does the admin utility support all 5 verticals" — confirmed, not fixed.** (a) `SuperAdminConsole.tsx`'s "Create Customer" dialog — the actual UI for `POST /api/admin-ui/customers`, whose backend is already correctly vertical-aware (threads `vertical` through `Customer`/`User`/`CustomerConfig`/`Account` consistently, no gap there) — has a Vertical `<select>` hardcoded to exactly two options: `dc2_s` ("Data Center (DC2_S)") and `saas` ("SaaS"). `datacenter_v1`, `manufacturing_iot`, and `healthcare_provider` are not selectable at all; someone wanting to admin-create a tenant on any of those 3 verticals today has to use the load-driver script or a raw API call, not this UI. Pure frontend gap — the backend already supports all 5. (b) `playbook_recommendations_api.py`'s `_CONFIG_PLAYBOOK_VERTICALS = {'dc2_s', 'datacenter_v1'}` gates its own KPI-threshold recommendation engine (PB-01 through PB-13) to just those two verticals — the approval-seeding path returns an honest no-op for the rest, `{'seeded': 0, 'reason': f'{vertical} not a config-playbook vertical'}`, not silently wrong. A separate, narrower, explicitly-commented `dc2_s` fallback exists for one edge case (manually triggering a specific `PB-XX` ID for a non-config-vertical customer, so the config *lookup* doesn't 404) — documented in its own code comment as intentional, not a live bug. **Correction, same day**: this item originally overstated (b) as "saas_premium/manufacturing_iot/healthcare_provider get zero playbook recommendations" — that was wrong, generalized from one gate to the whole system without checking. `verticals/saas_premium/vertical_config.py` is real, well-authored (6 playbooks: activation-blitz, voc-sprint, expansion-accelerator, renewal-safeguard, sla-stabilizer, health-check — built specifically to fix a prior bug per its own docstring, "FDE persona eval bugs B-4/B-5"), and actively imported by `_ask_ai_helpers.py`, `playbook_cost_bridge.py`, and `mcp_server/common.py`. (b) is real but narrower than stated: only `playbook_recommendations_api.py`'s specific KPI-threshold engine is gated to 2 verticals; saas_premium is fully served elsewhere. | confirmed, not fixed (scope corrected) |
| 42 | **A more precise, genuinely new "non-SaaS silently defaults to datacenter" bug, found while correcting item 41 — confirmed, not fixed.** `utils/vertical_playbook_routing.py`'s signal-triggered playbook routing (used by `signal_analyst` to auto-select a playbook when a qualitative signal like `champion_loss`/`escalation`/`nps_decline` fires) does a **binary** vertical check: `_playbook_map_for_vertical()` returns `_SAAS_PLAYBOOK_BY_SIGNAL if is_saas_vertical(vertical) else _DC_PLAYBOOK_BY_SIGNAL` — and `is_saas_vertical()` is just `'saas' in (vertical or '').lower()`. `manufacturing_iot` and `healthcare_provider` don't contain "saas," so both silently fall through to the **datacenter** signal→playbook map (`PB-DC-01` through `PB-DC-04`). A manufacturing_iot customer's `champion_loss` signal gets routed to `PB-DC-02` — a datacenter-specific playbook ID — not a manufacturing-appropriate one, and not an honest no-op either (unlike item 41(b)'s pattern). Same root-cause class as items 5/39/41: a two-way (or one-set-vs-everything-else) branch written when only 2 verticals existed, never updated as the registry grew to 5. Not yet fixed — needs either a real `manufacturing_iot`/`healthcare_provider` signal→playbook map (mirroring `_SAAS_PLAYBOOK_BY_SIGNAL`'s shape) or an honest no-op fallback, same shape as the decision facing item 39. **Fix shape, agreed 2026-08-28, not yet implemented**: replace the binary `is_saas_vertical()` check with a direct per-vertical dict lookup, the same proven-safe shape as `POWER_OF_1_PILLAR_MAPS` — `_SIGNAL_PLAYBOOK_MAPS = {'dc2_s': _DC_PLAYBOOK_BY_SIGNAL, 'datacenter_v1': _DC_PLAYBOOK_BY_SIGNAL, 'saas_premium': _SAAS_PLAYBOOK_BY_SIGNAL}`, `.get(vertical)` returning `None` for anything unmapped — forcing the caller to handle "no mapping for this vertical" as an honest no-op rather than silently guessing datacenter. Same fix shape should be considered anywhere else in the codebase still doing an `is_saas_vertical()`/substring-style vertical check instead of a direct registry lookup. | confirmed, not fixed — fix shape agreed |

**Items 30/32/33/34 — confirmed live on genuinely fresh writes (2026-08-27, customer 414, "Hyperion GPU Cloud", datacenter_v1).** Every prior confirmation for this batch (items 30/33 on 411/412, item 32 on 413) checked data that existed *before* the fix, or exercised only the arc-classification half of the pipeline. Customer 414 was registered, processed, extended, and had 2 playbook executions closed — the entire sequence run *after* `62f7c95b6` was already deployed — giving a true post-fix sample:
- **Item 30** (outcomes.csv evidence): all 40 `csv_import`-sourced OUTCOME nodes show `confidence=1.00`, `tier=1`, real non-empty evidence text — zero clamped-to-0.3 rows.
- **Item 33** (playbook-close OUTCOME clamp): all 9 `playbook_outcome` nodes (7 from `--register`'s auto-triggered PB-DC-01 executions, 2 from the `--extend --playbooks` close) show `confidence=1.00`, `tier=1`, real evidence text quoting the playbook ID and health values, and a populated `source_ref=playbook_execution:<execution_id>` — the pre-fix baseline on customer 408 was 100% `confidence=1.00/tier=1/evidence=NULL/source_ref=NULL` on the same node subtype, so this is the actual fix engaging, not a coincidental match.
- **Item 32** (arc_confidence no fabrication + retry): all 12 accounts show real, varied `arc_confidence` (0.55/0.75/0.8, matching their arc types) — zero NULL, zero suspicious 0.5-default clustering. Two `at_risk` accounts (3720, 3726, `exec_sponsor_change`, confidence 0.8) auto-triggered a real PB-04 decision, confirming a genuinely-classified value still reaches a live decision post-fix, not just the deferral half.
- **Item 34** (`key_decisions` revenue_impact honesty): DB-confirmed every fresh `DECISION` node on 414 has a genuinely-`NULL` `revenue_impact` (structurally correct — decisions don't carry a dollar amount); current source at `outcome_roi_engine.py:1534` computes `None` for these, not `0`, matching the fix. (Verified by direct source read + DB state rather than the live API route, which requires session auth the load-driver's client holds but this shell didn't have a working shortcut for in the time available — the DB+code combination is conclusive for this one since the defect was purely a Python conditional over already-NULL data.)

**Bonus, found incidentally during this run — new, minor, driver-side only:** `load-driver/scenarios/scenario_manifest.py:4075`'s extend-mode close-success log line has a malformed f-string — `f'...{health_now:.0f if health_now else "?"}...'` is not valid conditional syntax inside a format spec, so it raises `unsupported format string passed to NoneType.__format__` whenever `health_now` is `None` at close time. Caught by the surrounding `try/except`, so it doesn't block the actual close (confirmed: both closes succeeded server-side, `result['closed']` incremented before the crash), it just gets miscategorized as a `Close error` in the run summary and skips its `✅ closed` log line. Cosmetic, driver-only, not yet fixed.

---

## Documents — what each is actually for

| file | use it when |
|---|---|
| `state-of-play.md` | **start here** |
| `due-diligence-checklist.md` | walking a tenant end to end by hand |
| `tracer-app-prompt.md` | building the audit harness |
| `fix-load-generator-prompt.md` | refactoring the generator *(supersedes `synthetic-worldgen-prompt.md`)* |
| `generator-ground-truth-spec.md` | the `ground_truth.json` schema |
| `vertical-registry-architecture.md` | the registry refactor — **complete**: (a)-(c) and (e)-(f) shipped and tested, (d) deliberately paused as backlog; see Track B |
| `vertical-coupling-audit.md` | original findings — items 1–4 now fixed |
| `ws1-ws2-prompts.md` | edge provenance; WS-2's backfill half is cut (data disposable) |
| `fci-spike-prompt.md` | blocked on the generator — don't run it yet |
| `verify-three-findings.md` | F1/F2/F3 verification brief, unrelated to verticals |
| `edge-provenance-plan.md` | superseded by `ws1-ws2-prompts.md` |
| `causal_discovery_proto.py` · `cspulse_pipeline_sim.py` | teaching/reference code — `cspulse_pipeline_sim.py` is the reference implementation 2g's supersession rule was built from |
| `wizard-a-provenance.html` | the current-vs-target diagram |
| `03-edge-provenance/ws2/adjudication_matrix.md` | the actual WS-2 tier matrix (SIGNED v3/v4) — read before touching evidence_tier/supersession logic |
| `03-edge-provenance/ws2/node_evidence_gap.md` | node-side evidence gap notes, feeds into item 25/2f |
| `ws2/item26_two_models_decision.md` · `ws2/bucket_divergence_decision.md` | **historical** — working notes behind item 26/27's already-shipped Model C decision (`581be26ba`, 2026-08-24). Their own headers read like an open decision; they are not — check items 26/27 above first. |
| `../../WS2_2F_2G_SCOPING.md` (repo root `docs/`) | full 2f/2g engineering detail — decisions, citations, open questions — behind the Track C summary above |

---

## Launch bar

Nothing here has harmed a customer — there are none yet. The question is what must be true before the first real tenant:

1. No surface serves another vertical's data *(1–4 done; 5 open — a mislabeling, not a raw-data leak, which is worse to catch, not better: a partner seeing power metrics would notice, a CFO reading "Power & Facility — $500k" has no way to know it's GRR)*
2. Every customer-facing number carries provenance *(track A)*
3. Every causal edge carries a tier *(track C)*
4. Conformance tests exist and pass *(done for Track B — role-gate, catalog-shape, round-trip identity, and no-silent-substitution guards all exist and pass; item 17/22's residual has its own `xfail(strict=True)` marker rather than a passing test, which is the honest state, not a gap in this item)*

Everything else — learned schemas, decomposed retention models, FCI — is post-launch.

**Item 4 is done for the vertical-coupling class of bug.** The role-gate (`partner_portal` refusing on a null role) is solidly tested and independently confirmed by a tool that shares no code with this platform; the round-trip identity and no-silent-substitution guards Track B was missing are now shipped and deployed. What's still open on the launch bar is tracks A and C, and items 5/6/7/8/10-13/20/22 in the findings list above — none of which are "no conformance test exists," all of which are "a specific number is wrong or undecided."
