# ================================================================
# DEPRECATED — March 24, 2026
#
# This standalone Docker load driver is DEPRECATED.
# Use the main load driver directly instead:
#
#   cd load-driver/
#   python3 cs_pulse_driver.py --manifest manifests/granite_peak_dc2s.json --register --seed 42
#   python3 cs_pulse_driver.py --manifest manifests/granite_peak_dc2s.json -c 444 --phase baseline
#   python3 cs_pulse_driver.py --manifest manifests/granite_peak_dc2s.json -c 444 --phase intervention
#
# Why deprecated:
# - Old scenario-based CLI (1-9) replaced by manifest-driven V3 driver
# - Old credentials don't match current setup
# - Missing context graph scenarios (7/8) and 2-phase support
# - Main load-driver/ works locally and against any remote server via --base-url
#
# Available manifests:
#   manifests/granite_peak_dc2s.json    — 18 accounts, $68.7M ARR (DC2_S)
#   manifests/sandalwood_capital_dc2s.json — 18 accounts, $68.7M ARR (DC2_S)
#   manifests/novastar_dc2s.json        — 18 accounts, $62M ARR (DC2_S)
#   manifests/mont_blanc_saas.json      — 15 accounts, $42M ARR (SaaS Premium)
# ================================================================

version: '3.8'
# No services — file kept for reference only
services: {}
