# audit_account_column_access baseline
#
# Intentionally empty — drain complete.
#
# The 9 entries originally grandfathered in by PR #32 (the audit landing PR)
# have been fixed in a follow-up. New violations must be fixed at the call
# site (or, very rarely, deliberately added back here via:
#   python scripts/audit_account_column_access.py --regenerate-baseline
# ).
#
# Format: <relpath>:<base>.<attr>
