# Docker Environment Configuration
# Copy this to .env in backend/ directory

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=production
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5059

# Database
SQLALCHEMY_DATABASE_URI=sqlite:///kpi_dashboard.db
SQLALCHEMY_TRACK_MODIFICATIONS=False

# OpenAI API Key - REMOVED: Use customer-specific keys via Settings instead
# Keys are now stored encrypted per-customer in the database
# To set a key: Login > Settings > OpenAI Key Settings
# OPENAI_API_KEY=sk-proj-... (commented out - use Settings UI instead)

# RAG Configuration
RAG_TOP_K=10
RAG_SIMILARITY_THRESHOLD=0.3

# Qdrant Configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Upload Configuration
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216
SECRET_KEY=acb84a991046cb51965f6799a06520d7e43ea5283cdd027ba02aa889c9f968ec

# Encryption Key for API keys and credentials (Fernet encryption)
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=aM6Kck0iW3maiyqSCMNFgbL48M3sLCQ4bcIBwTHD4yE=
