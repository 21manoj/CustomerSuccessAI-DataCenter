# CS Pulse — Core Production Dependencies
# Install: pip install -r requirements-core.txt

# Web Framework
Flask==2.2.5
Flask-CORS==5.0.0
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.0.5
Flask-Session==0.8.0
Flask-Login==0.6.3
SQLAlchemy==2.0.23
alembic>=1.13.0
Werkzeug==2.2.3

# Data Processing
pandas==2.1.4
numpy==1.26.2
openpyxl==3.1.3
scikit-learn==1.3.0
# NRR Predictor v3 — sub-model GLMM fits (penalized GLM with offset trick;
# see predictor/glmm.py + PLAN_nrr_predictor_v3.md A1).
#
# Version pinning rationale (May 6 2026 deploy bug):
#   - statsmodels 0.14.4 imports `_lazywhere` from scipy._lib._util
#   - scipy >= 1.14 REMOVED _lazywhere
#   - Image was on scipy 1.16.3 → statsmodels imports failed at runtime
# Pin both to a compatible window. scipy 1.13.x is the last that
# still exports _lazywhere; statsmodels 0.14.4 supports scipy 1.8+.
scipy>=1.11,<1.14
statsmodels==0.14.4

# Environment & Config
python-dotenv>=1.0.0
pytz==2025.2

# HTTP & API Integrations
requests==2.31.0
boto3>=1.34.0,<2
httpx>=0.28.1,<1
anthropic>=0.39.0
openai>=1.2.0

# Security & Validation
cryptography
pydantic>=2.5.3,<3

# Database
psycopg2-binary==2.9.9

# Templates
jinja2==3.1.2

# Scheduling
apscheduler==3.10.4

# Rate Limiting
Flask-Limiter==3.5.0
limits==3.7.0

# Retry / Resilience
tenacity>=8.2.0

# WSGI Server (production)
gunicorn>=21.2.0

# Structured logging
structlog

# MCP Server (expose CS Pulse as tool provider for external LLMs)
fastmcp>=2.2.0
qdrant-client>=1.7.0
# Voyage AI embeddings via REST API (no SDK needed — avoids langsmith/langchain dep chain)

# Pinned: fakeredis 2.35+ removed FakeConnection import used by MCP server
fakeredis==2.34.1
